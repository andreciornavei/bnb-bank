{
    "private": true,
    "type": "module",
    "scripts": {
        "develop": "php artisan serve",
        "migrate": "php artisan migrate:fresh --seed"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
